# Store frozen ArchUnit violations in the source tree so the baseline is version-controlled.
# Path is relative to the Maven module working directory (sdk-core/).
freeze.store.default.path=src/test/resources/archunit_store
